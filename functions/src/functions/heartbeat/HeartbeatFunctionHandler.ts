import { HeartbeatFunction, HeartbeatFunctionAbstractHandler } from "amerykahospital-personalizedadvice-businesslogic";

export class HeartbeatFunctionHandler extends HeartbeatFunctionAbstractHandler {
    public async handle(input: HeartbeatFunction.Input) {
        return super.handle(input);
    }
}
