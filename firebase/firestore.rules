rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /ahpaa/pending_advices/ {
      allow read: if request.auth.uid != null;
      allow write: if request.auth.token.medicalproffesional == true;
    }
  }
}
